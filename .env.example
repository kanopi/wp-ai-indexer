# WordPress AI Indexer Configuration

# Required: WordPress site URL (without trailing slash)
WP_API_BASE=https://your-wordpress-site.com

# Required: OpenAI API Key
OPENAI_API_KEY=sk-...

# Required: Pinecone API Key
PINECONE_API_KEY=...

# Authentication (recommended if REST API is restricted)
# Use WordPress Application Password, not regular password
WP_API_USERNAME=your-admin-username
WP_API_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx

# Optional: Pinecone Configuration
# These can also be configured in WordPress admin settings
# PINECONE_INDEX_HOST=https://your-index.svc.region.pinecone.io
# PINECONE_INDEX_NAME=your-index-name

# Optional: Custom settings endpoint URL
# Default: ${WP_API_BASE}/wp-json/ai-assistant/v1/indexer-settings
# WP_AI_SETTINGS_URL=https://your-site.com/wp-json/ai-assistant/v1/indexer-settings

# Optional: Enable debug logging (1 = enabled, 0 = disabled)
# WP_AI_DEBUG=1

# Optional: Request timeout in milliseconds (default: 30000)
# WP_AI_TIMEOUT_MS=30000

# Optional: Concurrency level (default: 2)
# WP_AI_CONCURRENCY=2

# Optional: Pinecone namespace (leave empty for default)
# WP_AI_NAMESPACE=
